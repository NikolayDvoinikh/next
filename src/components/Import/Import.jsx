import styles from "./import.module.scss";

export const metadata = {
  title: "Import services",
  description: "Custom clearence, taxes, import, export",
};

export default function Import() {
  return <h3 className={styles.title}>Import</h3>;
}
